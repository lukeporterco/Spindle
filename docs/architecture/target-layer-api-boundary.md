# Target Layer API Boundary

This compatibility doc preserves the original lookup path for tests and local references.

The maintained Minecraft Target Layer boundary document now lives at:

- `docs/architecture/minecraft-target/target-layer-api-boundary.md`

That document still defines the planned layer split across:

- Injection Hook Subsystem
- Target Layer API
- Modding API

It also continues to reference:

- `com.spindle.core.minecraft.hook`
- `com.spindle.api.minecraft.target`
