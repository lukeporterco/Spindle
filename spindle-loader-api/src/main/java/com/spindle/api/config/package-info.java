/**
 * Stable Runtime API-0 config access for declared schema-2 mod configuration.
 *
 * <p>These types cover flat config keys materialized by Spindle before lifecycle execution. Calls
 * may raise {@link com.spindle.api.exception.ConfigAccessException} when the runtime config
 * contract does not allow the requested access.
 */
package com.spindle.api.config;
