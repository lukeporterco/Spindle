# SteelHook 0.1 Target Passes

This folder records Target-1 through Target-10, the Target-pass sequence that proves SteelHook 0.1.

Current status: SteelHook 0.1 proves the custom hook spine on one fake-server-only method-entry dispatcher path. It does not transform real Minecraft runtime artifacts, rewrite `StackMapTable`, expose a public hook API, add gameplay hooks, use Mixin or Java agents, or imply Java mod execution is sandboxed.

Primary closure document: `target-10-steelhook-hardening-caboose.md`.
