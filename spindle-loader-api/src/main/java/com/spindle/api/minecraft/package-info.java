/**
 * Deferred bootstrap-facing Minecraft server API placeholders.
 *
 * <p>These interfaces are used only by guarded Minecraft bootstrap fixtures today. They are not
 * part of the stabilized Runtime API-0 boundary and they are not the public Minecraft Modding API.
 * Spindle keeps Minecraft as a target, not the foundation, so this package remains outside the
 * current stable loader API surface.
 */
package com.spindle.api.minecraft;
